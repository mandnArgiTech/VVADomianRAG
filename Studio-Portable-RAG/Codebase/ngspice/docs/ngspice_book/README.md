# ngspice book (`docs/ngspice_book`)

This reference is **human- and LLM-authored** from ngspice C/H sources with line citations. **Book generator / scaffold scripts are not part of the workflow** (see [doument_prompt.md](../../doument_prompt.md)). RAG index scripts may maintain `rag_index.json` only; they do not emit these Markdown files.

Start at [INDEX.md](INDEX.md).
