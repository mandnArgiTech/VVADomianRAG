# Copy to .env in this directory or export in your shell (never commit .env).
DEEPSEEK_API_KEY=

# Optional: use deepseek-chat for research instead of deepseek-reasoner (faster iteration).
# CREW_FAST=1
