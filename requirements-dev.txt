# Dev / CI — tests and coverage for ingest.py
# Install pulls requirements.txt (aiohttp, pathspec, beautifulsoup4, …) so ingest tests match production imports.
# Run from repo root (uses pyproject.toml):
#   pip install -r requirements-dev.txt
#   pytest tests/
# If pytest fails loading unrelated global plugins (e.g. ROS launch_testing):
#   PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest tests/
-r requirements.txt
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-timeout>=2.3.0
